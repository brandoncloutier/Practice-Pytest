1. Collected 0 items means that 0 tests were ran. It will show green even if no tests were rendered. Also if it shows exit code 5 this means that the CI Pipeline returned "No tests were collected". This could mean there is a broken testpaths glob or the CI pipeline is broken.
2. The reason is that the pytest at collection time rewrites the abstract syntax tree and replaces the bare asserts with asserts that can give descriptive failure messages. With regular python asserts you would have to provide your own error messages and it would raise an AssertionError.
3. This would result in an Error. Failures are due to an error that occured within the test itself. Errors occur from something that happened outside of the test body itself. From the notes a collection failed could be an import error in the test file. Triaging errors matters versus failures because errors will block other tests from running and showing assertion errors?
4. Pytest will return an error because pytest wants to have its own __init__ func
5. pytest.ini is for configuration of how pytest should run. conftest.py is for configuration and fixtures that should be used within the tests themselves. No you would not put fixtures in side of pytest.ini. Whatever is defined in the conftest.py is automatically imported into any test file that is within the same directory